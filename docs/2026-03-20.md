# 2026-03-20 작업 일지

## 00:39 — 마트별/품목별 보기 검색 기능 추가

### 변경 파일
- `lib/features/shopping/presentation/screens/store_view_screen.dart` (수정)
- `lib/features/shopping/presentation/screens/item_view_screen.dart` (수정)
- `lib/features/shopping/presentation/screens/store_detail_screen.dart` (신규 포함)

### 작업 내용
- `StoreViewScreen`, `ItemViewScreen` 모두 `ConsumerWidget` → `ConsumerStatefulWidget` 전환
- AppBar 우측에 🔍 아이콘 추가 — 탭 시 AppBar가 검색 TextField로 전환
- X 버튼으로 검색 종료 및 입력 초기화
- 마트별 보기: 마트 이름 기준 필터링
- 품목별 보기: 품목 이름 기준 필터링
- 검색 결과 없을 시 "검색 결과 없음" 안내 문구 표시

### 커밋
`f39eb6b` — feat: 마트별/품목별 보기에 검색 기능 추가

---

## 작업 — 품목 수정/삭제 기능 추가

### 변경 파일
- `lib/features/shopping/presentation/screens/add_item_screen.dart` (수정)
- `lib/features/shopping/presentation/screens/store_detail_screen.dart` (수정)
- `lib/features/shopping/presentation/screens/item_view_screen.dart` (수정)

### 작업 내용
- `AddItemScreen`에 `initialItem` / `initialSession` 파라미터 추가 — 수정 모드 지원
- `initState()`에서 기존 품목 데이터로 폼 필드 초기화
- `_save()`에서 수정/추가 분기 처리 (`updateItem` vs `insertItem`)
- AppBar 타이틀 "품목 추가" / "품목 수정" 분기
- `StoreDetailScreen` 품목 행에 `⋮` 팝업 메뉴 (수정/삭제) 추가
- `ItemViewScreen`의 `_ItemGroup`을 `ConsumerWidget`으로 전환, 품목 행에 `⋮` 팝업 메뉴 (수정/삭제) 추가
- 삭제 시 확인 다이얼로그 표시

### 커밋
`107a3cb` — feat: 품목 수정/삭제 기능 추가
