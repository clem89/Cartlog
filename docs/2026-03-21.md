# 2026-03-21 작업 일지

## — Phase 4 영수증 자동 인식 기능 추가

### 변경 파일
- `pubspec.yaml` (수정) — `google_mlkit_text_recognition`, `image_picker` 패키지 추가
- `android/settings.gradle.kts` (수정) — AGP 8.7.3 → 8.9.1 업그레이드
- `android/app/src/main/AndroidManifest.xml` (수정) — 카메라/갤러리 권한 추가
- `lib/features/shopping/domain/models/parsed_receipt_item.dart` (신규) — 파싱 품목 임시 모델
- `lib/features/shopping/utils/receipt_parser.dart` (신규) — OCR 텍스트 파싱 유틸
- `lib/features/shopping/presentation/screens/receipt_scan_screen.dart` (신규) — 카메라/갤러리 선택 + OCR 실행 화면
- `lib/features/shopping/presentation/screens/receipt_review_screen.dart` (신규) — 파싱 결과 확인/수정/저장 화면
- `lib/features/shopping/presentation/screens/home_screen.dart` (수정) — "영수증 등록" 버튼 추가

### 작업 내용
- `ReceiptScanScreen`: 카메라 또는 갤러리에서 영수증 이미지 선택 → ML Kit으로 OCR 실행 → 파싱 후 리뷰 화면으로 이동
- `ReceiptParser`: 영수증 OCR 텍스트를 줄 단위로 파싱, 합계/세금/날짜 등 노이즈 라인 제거, 품목명+가격 추출
- `ReceiptReviewScreen`: 파싱된 품목 목록 표시, 개별 수정/삭제/선택, 마트명+날짜 입력, 선택된 품목 일괄 DB 저장
- 홈 화면에 "영수증 등록" 버튼 추가 (4번째 버튼)
- Android 카메라/갤러리 권한(`CAMERA`, `READ_EXTERNAL_STORAGE`, `READ_MEDIA_IMAGES`) 추가
- AGP 버전 충돌(androidx 의존성 요구 8.9.1+) 해결을 위해 업그레이드
- 에뮬레이터(Pixel 9 / Android 16) 빌드 및 실행 확인

### 커밋
`be6cf68` — feat: Phase 4 영수증 자동 인식 기능 추가
