# Cartlog — Claude 규칙

## Git
- 커밋 및 푸시는 사용자가 명시적으로 요청할 때만 진행한다 (예: "동기화해줘", `/sync`)
- 요청 없이 자동으로 커밋하거나 푸시하지 않는다
- 사용자가 명시적으로 커밋/푸시를 요청할 경우 확인 없이 바로 진행한다

## 코딩
- 작업 전 반드시 구현 계획을 먼저 보여준다 (파일 구조, 변경 내용 등)
- 사용자가 명시적으로 진행을 지시하기 전까지 코드를 작성하지 않는다
